# Copy to .env.local for local dev. On Vercel, set VITE_API_URL in Project Settings → Environment Variables.

# Public API base URL (no trailing slash). Required for production builds.
# Example: https://your-service-name.onrender.com
VITE_API_URL=http://localhost:5000
